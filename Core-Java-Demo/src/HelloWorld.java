public class HelloWorld {

	public static void main(String[] args) {
		System.out.println("Hello World");
		int num = 10;
		System.out.println("Number is "+ num);
	}
}

//Comment - Used to document code and ignored by Compiler
/* Workspace - is a directory on the disk used to group together a set of (somehow) related projects, 
some configuration pertaining to all these projects and some settings for Eclipse itself. */ 
/* Project - A project is a group of source files and the settings with which you build, run, and debug those source files. 
 In the IDE, all Java development has to take place within a project.*/
/* Package - A package in Java is used to group related classes. 
 * Think of it as a folder in a file directory. We use packages to avoid name conflicts, and to write a better maintainable code.
 * Like this System class lies into built-in java.lang package. We can also create our own packages. */
/* public - Access Modifier specifies the scope of a field, method, constructor, or class. 
Besides public we also have default, private and protected.*/
// class - A class is a blueprint or prototype from which objects are created.
/* {} - A block in Java is a group of one or more statements enclosed in braces. 
A block begins with an opening brace ({) and ends with a closing brace (}). 
Between the opening and closing braces, you can code one or more statements.*/
// public static void main(String[] args) - Java main method is the entry point of any java program
// method - A Java method is a collection of statements that are grouped together to perform a specific operation.
/* static - Non-Access Modifier is used for creating class methods and variables. 
Besides static we have final, abstract, synchronized and volatile modifiers*/
/* void - A method returns to the code that invoked it when it completes all the statements in the method, 
reaches a return statement, or throws an exception, whichever occurs first. Any method declared void doesn't return a value.*/
// String - Strings are a sequence of characters. The Java platform provides the String class to create and manipulate strings. 
/* [] - Arrays are used to store multiple values in a single variable, instead of declaring separate variables for each value.
String[] args; We have now declared a variable that holds an array of strings; this in main methods context is called as command line arguments*/
// Access Property and Method Invocation - Using DOT operator
/* ; - Java statements must end with a semicolon. The basic rule is that declaration 
and expression statements must end with a semicolon, but most other statement types do not.*/
// int - is a type of Integer Data Type. Data type specifies the size and type of values that can be stored in an identifier
// variable - A Java variable is a piece of memory that can contain a data value. A variable thus has a data type.
// = - Assignment operators are used in Java to assign values to variables

